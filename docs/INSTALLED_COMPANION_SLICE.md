# Installed companion/native slice

## Scope and observations

An owned Windows 11 development-candidate test exercised the real setup window's Install button, real current-user native registration, receipt2 and generated shortcut, visible installed companion, actual native stdio/IPC bridge, and the setup Uninstall button. It did not use a mock registration store.

Observed sequence:

1. Firefox/application preflight passed; this host's keys were absent in both registry views under HKCU and HKLM. A fresh owned local/profile domain was created; no normal browser profile was read or changed.
2. Setup's Install control produced a matching receipt2, manifest, helper/XPI hashes and immutable/external shortcut bytes. The independently resolved Programs folder was inside the isolated profile domain, not the normal Start Menu.
3. Setup retained its launched child and exposed that process identifier through its lifetime observation label. The driver used that observation to identify the companion window; it did not acquire cleanup authority through process-name/tree discovery. A running-engine status and actual tray rectangle were observed.
4. A separately retained native bridge process completed one 8 MiB loopback download. Independently computed output SHA-256 matched the deterministic fixture, a Completed event arrived, and reconnect returned exactly one completed task. Native EOF/reconnect did not replace the companion process. This is not independent crash-recovery proof.
5. The existing companion Quit control stopped it. Setup observed exit and joined its retained child; the tray and runtime publication disappeared. The fixture server and native bridge/readers were retired and joined.
6. Setup's Uninstall control removed matching native registration, generation files and shortcut group while preserving the output. Setup then closed and its retained process was joined; registry absence and closed-application checks passed again. Owned fixture files were retained as evidence.

All control actions were synthetic native control messages, not physical input. No Firefox process was launched, no XPI was installed, and no ordinary browser download click or normal Start Menu launch was tested. The package was a dirty-worktree development candidate based on3e94b60 with setup lifetime-observation changes; neither3e94b60 nor later source inherits exact-head installed qualification from this observation. Package/source/report identities remain distinct.

## Retained failures and ownership observations

The first attempt failed before installation: the child environment resolved Programs beneath the isolated USERPROFILE, but that directory had not been prepared. The setup coordination lock existed; the install root and native registration did not. A metadata-only check confirmed that Programs resolved inside the owned domain and did not exist. The failed fixture and its setup window were preserved; the original driver had released its process handle and could not subsequently infer forced-retirement authority from a PID. This is a cleanup deficiency of that failed driver, not a successful containment result.

A second fixture guard incorrectly required Programs beneath the APPDATA spelling; it refused before launching setup. The subsequent fixture independently resolved Programs beneath the owned domain, created that fixture directory, and passed the sequence above. Production's missing/unsafe-directory refusal was not weakened. Later passes do not retroactively supply cleanup or success for either failed attempt.

Setup now exposes retained launch/exit observations separately from its operation status. The child is retained before publishing its identifier; failed waits retain ownership, and a joined-exit observation is emitted only when no launched child remains. These observations do not make setup's launch request a tray-readiness receipt or make a PID standalone ownership authority.

## Operation observations and driver cleanup

Setup label312 starts at `Operation 0: idle`. Each accepted action advances a per-window counter before configuration or worker startup and reports `running`; `complete` is published after configuration refusal or after joining/accepting the worker result and retaining any launched child. Completion is not operation success, tray readiness, durable receipt2 or a cross-process transaction identifier. Ignored requests do not advance the counter.

The diagnostic `SetupOwner` records dispatch uncertainty before sending a control. It never replays a failed delivery. Initial no-child text does not resolve a pending request; cleanup requires the expected completed operation plus no retained Manager (or a joined-child observation). Normal Manager Quit must precede setup Close. Failed waits retain the exact process object; a late exit can be joined without another Close request. Unknown observations preserve the domain and refuse success rather than killing a discovered process. These callbacks require the same retained setup instance and a controlled fixture, not arbitrary concurrent GUI actions.

`DomainPlan` writes a local plan before exclusive domain creation. A plan is not a creation witness and cannot adopt a collision. The no-install window driver now uses these helpers, records status/lifetime before window destruction, and attempts retirement even if failure-record writing fails. Its normal read-only refusal and injected failures before window discovery/after refusal/while writing failure diagnostics were observed with exact retained setup waits and no forced termination; failed cases produced no success report. These checks neither exercise registration nor qualify installed failure recovery. The installed entry point below now composes these helpers; the available-machine observations below now cover its real-registration sequence and nine fault cases, with an explicit exit-status assertion limit.

## Reviewed driver composition, not installed qualification

`scripts/test-installed-companion.py` is an opt-in maintainer entry point for a fresh isolated local/profile/Programs domain. It uses real setup controls and current-user registration, not CLI installation or a mock store. It refuses live Firefox/helpers, other setup instances, existing registration in either hive/view, path aliases, non-paired package metadata and a missing passive operation-label compatibility hint. The hint and matching hashes are not publisher authenticity or observed readiness. No normal browser profile or normal Start Menu is used.

`SetupOwner.quiesce()` settles the accepted operation and requests/join-observes Manager Quit while retaining setup for Uninstall; `retire()` additionally closes/waits setup. The driver retires native parents/readers and fixture resources independently, requires current receipt/package/link/registration agreement again before Uninstall, and never replays an uncertain uninstall. A failed resource retirement prevents registration removal but does not skip Manager Quit. Unknown journals, changed bindings and unknown entries are preserved with failure, not repaired or deleted by a raw registry fallback. Constructor failures retain native and HTTP owners; fixture socket cleanup also covers failure before its thread starts. Finished HTTP handler references are joined before pruning. Diagnostic records contain bounded classifications and owned lifecycle observations, not URLs, credentials or native message dumps.

The intended positive sequence is setup Install → observed tray/engine → one independently verified8MiB loopback output and Completed → native EOF/reconnect with the same companion and one task → joined native/fixture retirement → Manager Quit/parent join → setup Uninstall/output preservation → setup Close/wait. Nine fault checkpoints span domain creation through uninstall. Fourteen driver-policy tests include a modeled positive sequence and all nine modeled failures, real isolated HTTP socket constructor refusals, partial native-reader constructor ownership, binding/journal/unknown-file refusal and live-app/uncertain-action gates. Seven targeted driver mutations were rejected. These tests do not establish installed execution, native-pump process-tree retirement after forced parent loss, cold-start reliability or Firefox behavior. The shared native fixture's default environment remains unchanged; an explicit owned environment is used by the installed driver.

Maintainer invocation after reviewing the exact paired candidate and satisfying fresh preflights (not the end-user quick start):

```powershell
python scripts/test-installed-companion.py --package artifacts/<reviewed-paired-package> --report artifacts/<new-report>.json --execute-owned-install
```

`--fail-at` explicitly selects an expected failure; it never authorizes a success report. If cleanup remains unconfirmed, the CLI retains live known actors in a failure-only holding phase instead of discarding their handles on exit. It may outlast the failed observation budget, but never retries Install/Uninstall or converts the failure into a pass. Use the visible Manager Quit before Close setup; externally terminating the driver forfeits retained ownership and does not prove cleanup. An unexpected setup exit does not prove Manager or native-pump process-tree retirement. Do not automate a sweep or recover from old tickets: plans are not creation witnesses or process ownership. A successful no-install window check or passive package check is not this installed sequence. Available-machine real-registration observations are recorded below; they do not close the remaining browser/final-package gates.

## f0b538c driver / 1c49add paired-package observations

On Windows11 build26200 x64, clean harnessf0b538c exercised clean paired-package1c49add through the five recorded checks: real GUI install/receipt2/registration/isolated shortcut and tray; one independently verified8MiB output and Completed; EOF/reconnect with one task and the same companion; native/fixture retirement and Manager Quit/join with tray/runtime disappearance; GUI uninstall with output preservation and exact setup wait. Fresh closed-app/all-view registration-absence checks passed afterward. The package descriptor SHA-256 was `d2f3af4829a31430da56e98a1ce1736eee8dcca2e117dce9ca79019aab056028`; output SHA-256 was `7d212b9c884f5c77896de960ae17cc341cda43b14d6a971f34ca29ebd4badf7f`. These are two distinct source identities, not qualification of subsequent bytes.

All nine fault checkpoints then produced the expected failure, no success report and no reported cleanup errors. Domain creation started no setup process; the other eight joined setup, and the seven cases after installation observed verified uninstall. Each case was followed by fresh closed-app and registration-absence checks. Fixture files were preserved. The first batch controller stopped because its expected supervisor-record shape omitted the actual `qualification:false` field; independent inspection confirmed the first fault's expected result before the remaining eight ran. That controller assertion failure remains a failure, not a product defect or a silent successful batch.

Evidence review identified a missing positive-path distinction: f0b538c accepted both successful and failed-but-joined Manager exit during quiescence. Its recorded download/reconnect/retirement observations remain valid, but it did not independently require a successful Manager exit. The nominal driver now requires the exact successful-exit observation; failure cleanup still accepts joined failed exit. A modeled failed-exit case and omitted-assertion mutation reject the weaker condition. A new real nominal run is required for that strengthened check; earlier evidence is not relabeled. These tests used synthetic native controls and isolated Programs, not Firefox, persistent XPI, physical input, normal Start Menu or cold-start qualification.

## Remaining gates

Persistent signed XPI, Firefox restart/native-parent lifetime, supported ordinary-click capture, normal Start Menu/physical tray interaction and exact-final-main package qualification remain open. The strengthened nominal exit-success check still needs a fresh installed run; existing legacy drivers must not be relabeled as receipt2/Firefox qualification. The failed setup window is outside the successful run's cleanup result.

References: [SHORTCUT_OWNERSHIP.md](SHORTCUT_OWNERSHIP.md), [COMPANION_DESIGN.md](COMPANION_DESIGN.md), [USER_WORKFLOW.md](USER_WORKFLOW.md).
