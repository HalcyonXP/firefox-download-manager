# v0.1.0 — qualified Windows x64 personal release

Published **2026-09-09T15:09:37Z**: [release, installation notes and assets](https://github.com/HalcyonXP/firefox-download-manager/releases/tag/v0.1.0).

This record closes the #28 → #46 qualification/publication sequence in ADR0012. Later publication-tool/documentation commits and their CI candidates do **not** replace the frozen product/tag below. The release is MIT licensed, unsigned, temporary-XPI based and qualified only for the recorded environment/limits—not publisher authentication or a signed permanent Firefox installation.

## Frozen inputs

- Product source and all actual local qualification drivers: `d03a56c373bfee37776d23031908a93fc68da89a`.
- Annotated tag object: `70057979493534253e8a4c7574186f8d0598fa9b`, peeling to that source.
- Authoritative product-main CI: [34357070862](https://github.com/HalcyonXP/firefox-download-manager/actions/runs/34357070862), all three jobs passed.
- Separate publication auditor: `e24c30f02ceac4d2a8a0748655ffdb9a85a377ea`; [CI34364557739](https://github.com/HalcyonXP/firefox-download-manager/actions/runs/34364557739) passed all three jobs. CI34363545718 at59117fb was superseded/cancelled, not passed.

| Input | SHA-256 |
| --- | --- |
| Released ZIP | `ff67edcd4713bfe37407970645c51527d9a99e97837d089304a192499bcfd62e` |
| Package descriptor | `cc3a40e68b74eebaceab586cef5c6a3230c4963a1a48bf4da6ea2d70e94abc2a` |
| Helper | `d7d7076769c6cb9f219fc4224fee1b1ff92b92a41c8ef1aab771872734c79bc3` |
| Setup | `04b0a4eb0cbb53240a27d1b103b5d619788a446d029e24f64ebc51e87d5bff40` |
| XPI | `38521d8c4055bd5dcc53c2277316382bf8910100a4e303d10165179006ce0cbe` |
| Bounded `QUALIFICATION.json` review record | `3b3b1849e2b39b55ffe2768f9e3c6e3f91d19835f67553e63a3a296e3a158878` |

The ZIP is the exact downloaded main artifact, not a local rebuild. Its eight payloads, nine XPI leaves, MIT/esbuild notices, descriptor/payload hashes and same-environment repeat build were checked. Cross-environment repeatability is unestablished.

## Qualification and cleanup

Clean-d03 drivers passed installer7, native26/default2GiB and actualFirefox20 against those exact bytes on nativeAMD64 Windows11 build26200 / Developer Edition156.0 aurora. `QUALIFICATION.json` embeds the unchanged bounded component reports and original report hashes. [RELEASE_MATRIX.md](../RELEASE_MATRIX.md) maps the separate Rust, compiled-host, artifact, actual-browser and policy layers; these counts do not mean every library adversary ran through Firefox.

Real toolbar/link creation, monitoring/settings, controls/validation cancellation, exact output, CSP/private capability/non-submission, UI-port reconnect, Firefox/helper durable-prefix restart, renewed-session fresh Add and same-artifact setup lifecycle passed. The earlier native retained-handle crash test and the browser's full restart remain distinct. Signing readback was `value:true, user:false`. Every owned process/fixture and setup cleanup joined before successful reports; normal browser profiles were neither inspected nor modified. The owner may reopen Firefox. No normal-profile installation was performed automatically.

The2GiB loopback observation was2.5231771s elapsed/2.328125s helper CPU,41,766,912bytes peak working set and39,432,192bytes peak sampled private usage,25 samples at100ms spacing,10 events/5,542bytes. This excludes OS cache/kernel/browser/other processes; aggregate I/O is not disk-only. No Internet/VPN/physical-device throughput or product memory/latency ceiling is claimed.

A separate clean OS and developer-tools-free installation were unavailable, not passed. Hosted Server-x64 and Windows11 ARM64 x64-emulation evidence is supplemental. Other OS/browser/hardware deployments, prior-released-version upgrade, arbitrary power loss and hostile same-account isolation are not established. The release notes retain these limits, optional-session recovery rules, static-private-document versus capability denial, non-cancellable promotion and unsigned/temporary-XPI reload instructions.

## Publication and privacy evidence

The corrected clean auditor reconciled28 issues/19 PRs before publication; its snapshot checked39 refs/78 commits/642 historical blobs/182 paths and rejected all26 original-commit lookups. Raw annotated-tag metadata/ref and selected release inputs were reviewed separately; commit traversal alone is not a tagger/message inspection. The public-authority private archive remains private and was not imported or declared content-cleared.

The final prepublication archive snapshot covered94 newly downloaded archives plus54 same-ID/current-API-digest matching previously reviewed artifacts:798 newly scanned streams/508 unique payloads, no findings/gaps/new contacts among36 previously adjudicated contact candidates. These fresh-scan counts are not cumulative or guaranteed monotonic. Seven caches, then one post-tool-CI cache, were inventoried/deleted and absence verified; **deletion is not inspection**. Later CI/caches/inputs require their own scoped review.

Five selected initial files (ZIP, bounded review, release checksum, notes and raw tag metadata) received nested ZIP/UTF8/UTF16LE review:24 streams/23 unique payloads, no findings/new contacts. Draft upload/readback and metadata review passed before publication. Anonymous public API/source-tree/tag/notes and downloaded asset bytes then matched the reviewed hashes.

Closeout caught a checksum-filename mismatch: installed `INSTALL.md` names the original builder `PACKAGE-SHA256SUMS.txt`, but initial publication exposed only the additional release-wide `SHA256SUMS.txt`. The original builder checksum was separately reviewed and added as a **fourth** attachment. No tag, ZIP, existing asset, qualification record or release-note bytes were replaced. A fresh anonymous readback verified all four assets. The initial three-asset check remains narrower history.

- Release `PACKAGE-SHA256SUMS.txt`: the builder's ZIP checksum, named by installed instructions.
- Release `SHA256SUMS.txt`: ZIP plus `QUALIFICATION.json` checksums.
- Inside the extracted ZIP, `SHA256SUMS.txt`: package payload checksums, a different scope.

The metadata-count discrepancy and first corrected-audit refusal lacked retained raw responses; later success does not reconstruct their causes. Missing-pull counterexamples/mutations establish explicit coverage, while bounded diagnostics and clean stable-ref audits preserve refusal rather than weakening it. One clean audit refused final consistency during the post-push interval; its exact failed predicate is unavailable. Subsequent completed-CI/stable-ref context and clean pre-tag/tagged-draft audits passed. See [PUBLICATION_PRIVACY.md](../PUBLICATION_PRIVACY.md).

An ad-hoc evidence assembly also initially passed a string where `new_report` requires `Path`; it stopped before writing a report. Only its known empty output directory was reused after correcting that caller type. This was not a browser/setup rerun or a passed qualification report. Earlier fixture, installation-interruption and CI failures remain documented in their original scopes.
